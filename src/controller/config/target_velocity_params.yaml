/**:
  ros__parameters:
    # Target velocities and its corresponding durations
    target_velocities_mps:               [0.0, 0.3, 0.5, 0.3, 0.0]
    durations_s:                         [2.0, 5.0, 8.0, 5.0, 2.0]

    # Node/publish frequency
    target_velocity_update_frequency_hz: 20.0

    # Flag for looping over the target velocity vectors
    loop:                                true
